#ifndef YAMBDA_FUNCS_MATH
#define YAMBDA_FUNCS_MATH

#include "../types.h"

Element *math_sum(Env *env, Element *x);
Element *math_mul(Env *env, Element *x);
Element *math_div(Env *env, Element *x);

#endif
