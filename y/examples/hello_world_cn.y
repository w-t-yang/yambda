let x "你好"

let 让 let
let 如果 cond
let 等于? =?
let 定义 def
let 对于每一个 foreach
let 打印 print


定义 测试函数
    让 n 等于: $1
    对于每一个 (` 1 2 3)
        让 n 等于: (+ n $_)

    打印 "数字 n:" n

    如果 (等于? n 6)
        打印 x $2 "n的值是" n
        否则, 打印 x $2 "n的值是" n

测试函数 0 "世界"
测试函数 n: 1 user: "陌生人"
